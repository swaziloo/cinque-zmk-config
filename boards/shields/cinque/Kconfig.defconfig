if SHIELD_CINQUE_LEFT

config ZMK_KEYBOARD_NAME
    default "cinque"

config ZMK_SPLIT_ROLE_CENTRAL
    default y
endif

if SHIELD_CINQUE_LEFT || SHIELD_CINQUE_RIGHT

config ZMK_SPLIT
    default y

endif
